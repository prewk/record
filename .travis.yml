language: php
php:
    - 7.0
    - 7.1
    - 7.2

before_script:
    - composer selfupdate
    - composer install --prefer-source

script:
    - mkdir -p build/logs
    - phpunit -c phpunit.xml.dist

after_script:
    - travis_retry php vendor/bin/coveralls -v